build-service:
  build:
    image: eslint-config
    dockerfile: Dockerfile
    encrypted_args_file: codeship-args.encrypted
  encrypted_env_file:
    - codeship-env.encrypted
  volumes:
    - ./.codeship/.ssh:/root/.ssh
  cached: true
